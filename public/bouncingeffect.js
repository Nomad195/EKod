var rightarrow0 = document.getElementById("rightarrow0");
var rightarrow1 = document.getElementById("rightarrow1");
var rightarrow2 = document.getElementById("rightarrow2");
var rightarrow3 = document.getElementById("rightarrow3");
var rightarrow4 = document.getElementById("rightarrow4");
var rightarrow5 = document.getElementById("rightarrow5");
var rightarrow6 = document.getElementById("rightarrow6");
var rightarrow7 = document.getElementById("rightarrow7");
var rightarrow8 = document.getElementById("rightarrow8");
var rightarrow9 = document.getElementById("rightarrow9");
var rightarrow10 = document.getElementById("rightarrow10");
var rightarrow11 = document.getElementById("rightarrow11");




var leftarrow0 = document.getElementById("leftarrow0");
var leftarrow1 = document.getElementById("leftarrow1");
var leftarrow2 = document.getElementById("leftarrow2");
var leftarrow3 = document.getElementById("leftarrow3");
var leftarrow4 = document.getElementById("leftarrow4");
var leftarrow5 = document.getElementById("leftarrow5");
var leftarrow6 = document.getElementById("leftarrow6");
var leftarrow7 = document.getElementById("leftarrow7");
var leftarrow8 = document.getElementById("leftarrow8");
var leftarrow9 = document.getElementById("leftarrow9");
var leftarrow10 = document.getElementById("leftarrow10");
var leftarrow11 = document.getElementById("leftarrow11");



var MC0 = document.querySelector("#MC0");
var MC1 = document.querySelector("#MC1");
var MC2 = document.querySelector("#MC2");
var MC3 = document.querySelector("#MC3");
var MC4 = document.querySelector("#MC4");
var MC5 = document.querySelector("#MC5");
var MC6 = document.querySelector("#MC6");
var MC7 = document.querySelector("#MC7");
var MC8 = document.querySelector("#MC8");
var MC9 = document.querySelector("#MC9");
var MC10 = document.querySelector("#MC10");
var MC11 = document.querySelector("#MC11");
rightarrow0.addEventListener("click", function () {


    // -> removing the class
    MC1.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC1.offsetWidth = MC1.offsetWidth;

    // -> and re-adding the class
    MC1.classList.add("bounce");
}, false);

rightarrow1.addEventListener("click", function () {


    // -> removing the class
    MC2.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC2.offsetWidth = MC2.offsetWidth;

    // -> and re-adding the class
    MC2.classList.add("bounce");
}, false);

rightarrow2.addEventListener("click", function () {


    // -> removing the class
    MC3.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC3.offsetWidth = MC3.offsetWidth;

    // -> and re-adding the class
    MC3.classList.add("bounce");
}, false);


rightarrow3.addEventListener("click", function () {


    // -> removing the class
    MC4.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC4.offsetWidth = MC4.offsetWidth;

    // -> and re-adding the class
    MC4.classList.add("bounce");
}, false);


rightarrow4.addEventListener("click", function () {


    // -> removing the class
    MC5.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC5.offsetWidth = MC5.offsetWidth;

    // -> and re-adding the class
    MC5.classList.add("bounce");
}, false);

rightarrow5.addEventListener("click", function () {


    // -> removing the class
    MC6.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC6.offsetWidth = MC6.offsetWidth;

    // -> and re-adding the class
    MC6.classList.add("bounce");
}, false);

rightarrow6.addEventListener("click", function () {


    // -> removing the class
    MC7.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC7.offsetWidth = MC7.offsetWidth;

    // -> and re-adding the class
    MC7.classList.add("bounce");
}, false);


rightarrow7.addEventListener("click", function () {


    // -> removing the class
    MC8.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC8.offsetWidth = MC8.offsetWidth;

    // -> and re-adding the class
    MC8.classList.add("bounce");
}, false);

rightarrow8.addEventListener("click", function () {


    // -> removing the class
    MC9.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC9.offsetWidth = MC9.offsetWidth;

    // -> and re-adding the class
    MC9.classList.add("bounce");
}, false);


rightarrow9.addEventListener("click", function () {


    // -> removing the class
    MC10.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC10.offsetWidth = MC10.offsetWidth;

    // -> and re-adding the class
    MC10.classList.add("bounce");
}, false);

rightarrow10.addEventListener("click", function () {


    // -> removing the class
    MC11.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC11.offsetWidth = MC11.offsetWidth;

    // -> and re-adding the class
    MC11.classList.add("bounce");
}, false);

rightarrow11.addEventListener("click", function () {


    // -> removing the class
    MC0.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC0.offsetWidth = MC0.offsetWidth;

    // -> and re-adding the class
    MC0.classList.add("bounce");
}, false);




































































































leftarrow0.addEventListener("click", function () {


    // -> removing the class
    MC11.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC11.offsetWidth = MC11.offsetWidth;

    // -> and re-adding the class
    MC11.classList.add("bounce");
}, false);

leftarrow1.addEventListener("click", function () {


    // -> removing the class
    MC0.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC0.offsetWidth = MC0.offsetWidth;

    // -> and re-adding the class
    MC0.classList.add("bounce");
}, false);

leftarrow2.addEventListener("click", function () {


    // -> removing the class
    MC1.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC1.offsetWidth = MC1.offsetWidth;

    // -> and re-adding the class
    MC1.classList.add("bounce");
}, false);


leftarrow3.addEventListener("click", function () {


    // -> removing the class
    MC2.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC2.offsetWidth = MC2.offsetWidth;

    // -> and re-adding the class
    MC2.classList.add("bounce");
}, false);


leftarrow4.addEventListener("click", function () {


    // -> removing the class
    MC3.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC3.offsetWidth = MC3.offsetWidth;

    // -> and re-adding the class
    MC3.classList.add("bounce");
}, false);

leftarrow5.addEventListener("click", function () {


    // -> removing the class
    MC4.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC4.offsetWidth = MC4.offsetWidth;

    // -> and re-adding the class
    MC4.classList.add("bounce");
}, false);

leftarrow6.addEventListener("click", function () {


    // -> removing the class
    MC5.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC5.offsetWidth = MC5.offsetWidth;

    // -> and re-adding the class
    MC5.classList.add("bounce");
}, false);


leftarrow7.addEventListener("click", function () {


    // -> removing the class
    MC6.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC6.offsetWidth = MC6.offsetWidth;

    // -> and re-adding the class
    MC6.classList.add("bounce");
}, false);

leftarrow8.addEventListener("click", function () {


    // -> removing the class
    MC7.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC7.offsetWidth = MC7.offsetWidth;

    // -> and re-adding the class
    MC7.classList.add("bounce");
}, false);


leftarrow9.addEventListener("click", function () {


    // -> removing the class
    MC8.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC8.offsetWidth = MC8.offsetWidth;

    // -> and re-adding the class
    MC8.classList.add("bounce");
}, false);

leftarrow10.addEventListener("click", function () {


    // -> removing the class
    MC9.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC9.offsetWidth = MC9.offsetWidth;

    // -> and re-adding the class
    MC9.classList.add("bounce");
}, false);

leftarrow11.addEventListener("click", function () {


    // -> removing the class
    MC10.classList.remove("bounce");

    // -> triggering reflow /* The actual magic */
    // without this it wouldn't work. Try uncommenting the line and the transition won't be retriggered.
    MC10.offsetWidth = MC10.offsetWidth;

    // -> and re-adding the class
    MC10.classList.add("bounce");
}, false);